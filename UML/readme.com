A sequence diagram program that is simple. Open in application downloaded from 

https://sourceforge.net/projects/sdedit/

It is a Jar file run by:

java -jar sdedit-4.2-beta1.jar

It reads text files using File -> Open if the file has a file.sd format.

Sample is in the folder
